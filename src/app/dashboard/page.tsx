import { DashboardPage } from '@/components/blocks/pages/dashboard-page'

export default function Page() {
  return (
    <DashboardPage
      workspaces={[]}
      flows={[]}
      tasks={[]}
      agents={[]}
    />
  )
}
